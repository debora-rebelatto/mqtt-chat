{
  "name": "mqtt-chat",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build ./src/index.ts --outdir ./dist",
    "test": "bun test",
    "test:communication": "bun run src/test/communication-test.ts",
    "test:real": "bun run src/test/multi-client-test.ts",
    "test:all": "bun run test:communication && bun run test:real"
  },
  "devDependencies": {
    "@types/bun": "^1.2.21",
    "prettier": "3.6.2"
  },
  "peerDependencies": {
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@types/paho-mqtt": "^1.0.10",
    "crypto": "^1.0.1",
    "mqtt": "^5.14.1"
  }
}